import React from 'react';
import styles from '@/app/(public)/team/[teamId]/mypage/mypage.module.scss';

const LeavingReportContainer = () => {
  return (
    <div className={styles.mypage_leaving_report}>
      <h3 className={styles.mypage_leaving_report_title}>退勤報告</h3>
    </div>
  );
};

export default LeavingReportContainer;
