import React from 'react';
import styles from '@/app/(public)/team/[teamId]/mypage/mypage.module.scss';

const TimeBoxContainer = () => {
  return (
    <div className={styles.mypage_time_box}>
      <h3 className={styles.mypage_time_box_title}>タイムボックス</h3>
    </div>
  );
};

export default TimeBoxContainer;
